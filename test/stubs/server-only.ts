// Test stub for the `server-only` build marker. In production Next resolves
// `server-only` via the react-server condition; under the node test runner it
// has no real module, so this no-op stands in (see vitest.config.ts alias).
export {};
